class Category {
  final String id, title, imageUrl;

  const Category(
      {required this.id, required this.title, required this.imageUrl});
}
// List<Category> data=[Category(id: id, title: title, imageUrl: imageUrl)]